type Value @model @auth(rules: [{ allow: owner }]) {
    id: ID!
    name: String!
    description: String
}

type Project @model @auth(rules: [{ allow: owner }]) {
    id: ID!
    name: String!
    description: String
}
